# 第一批迁移验收记录

验证日期：2026-09-06（Asia/Shanghai）。环境：Windows、Node.js 24.14.0、Next.js 16.3.4、React 19.2.8、Playwright Chromium。

## 已验证

| 检查 | 结果 |
| --- | --- |
| 旧 UI 源文件基线 | 64 个文件未改变，无新增/删除 |
| 原后端 TypeScript 构建 | 通过 |
| 原 Astro 检查和构建 | 通过，0 错误、0 警告 |
| 新版 TypeScript 检查 | 通过 |
| 新版生产构建 | 通过，3 个漫画业务路由 |
| 单元测试 | 4/4 通过 |
| 开发版浏览器回归（首轮完整集） | 21/21 通过 |
| 生产版浏览器回归（最终扩充集） | 23/23 通过 |
| 本地真实 API 只读检查 | 新版能读取现有 35 个漫画源，后台页面请求返回 200 |
| 并行启动脚本 | 能识别并复用已运行的三个服务 |

生产版 23 项中，18 项是旧版/新版配对截图：桌面与手机各 9 个页面状态。其余覆盖源筛选/选择/弹窗关闭/搜索、过期请求隔离、跨新旧页面往返后搜索、发现请求不重复、API 查询参数/Cookie/多条 Set-Cookie/图片响应代理。

18 组配对截图的页面尺寸全部一致，差异像素占比全部低于 0.1%；本轮最大约 **0.0514%**（手机源选择弹窗，169/329160 像素），并非宣称所有像素完全相同。没有遮蔽业务区域，动画和光标统一关闭。

原图、候选图、差异图、逐项统计和失败 trace 由测试生成在本地 `apps/web/test-results`，报告在 `apps/web/playwright-report`；这些可重新生成的本地文件不提交 Git。

## 不在本轮通过范围

- 没有宣称真实 Venera 源的所有详情/章节/图片规则已经修复。
- 没有验证或迁移全部阅读器、漫画架、收藏同步和后台操作。
- 没有迁移 PostgreSQL，未实施正式站点切换。
- HTTP 200 不等于后台全部交互完成回归；这些页面仍由原站提供。

下一阶段必须继续沿用“先建立对应页面基线，再迁移，再对照”的流程。功能和数据迁移的验收不可用本轮截图结果替代。
