# 全项目持续重构执行记录

用户要求：保持 UI 和全部功能，每完成一阶段报告进度，持续实施直到全部完成。内部阶段/提交用于验证和回退，不作为停止点。

## 已验证基础

- 漫画首页、搜索、发现：Next/React + 原 UI 配对截图。
- 图片上下文、响应解密、条带重排、精确章节：17 项测试通过。
- 原 29 项浏览器回归包括 18 组新旧 UI 截图；实际源首图验收见 `MANGA-ENGINE.md`。

## 当前执行

### 单服务生产部署（已验收）

`bash scripts/deploy.sh --pull` 现在直接拉取 `origin/main`，安装并构建 `backend` 与 `apps/web`，启动 `boke-backend` 和 `boke-web`，并自动移除旧的 `boke-frontend` PM2 进程。Next 生产端口沿用 3002，API 使用 3001；Next 不再回退代理到 Astro。原 Astro 源码保留作迁移对照，不参与生产启动。

静态资源已迁入 `apps/web/public`，离线 Service Worker 只预缓存新版实际路由，不依赖旧站的 `/archive`、`/nav` 等路径。验证：Next 类型检查、生产构建、前端单元测试 10/10、完整生产浏览器回归 82/82；回归包含后台、漫画详情/阅读器、漫画架/收藏、阅读中心、离线阅读和写作台。

限制：全站其他页面尚未迁移时不会由旧站接管；生产部署这些 URL 会返回新版 404。SQLite、数据库备份、上传目录和 `.env` 仍需保留，源执行隔离和 PostgreSQL 迁移仍未完成。

### 漫画源持久化

已实现仓库领域模块、专用状态/脚本表、源码哈希与版本缓存、设置/数据/Cookie 持久化、配置导入导出与启停/设置管理接口、备份表清单接入。重新同步保留停用标记，导出的配置不含会话凭据。旧接口继续兼容，数据库仅新增表，不删除或迁移现有业务数据。

验证命令：`npm run test:manga-storage --prefix backend`（4 项），`npm run test:manga-engine --prefix backend`（17 项）。源状态恢复测试使用重建 store/map 和运行时模块模拟重启，不把它描述为操作系统进程重启演练。

限制：当前存储适配器仍为 SQLite；公开源运行时仍是站点共享实例，不表示已实现每访客登录隔离。Cookie/源状态保存在私有表中而非公开 settings。移除仓库不删除历史状态，便于恢复；重新导入同 ID 会复用其状态。脚本缓存每次显式同步仓库失效；首个成功初始化后再写缓存，不缓存执行失败的脚本。

新增接口位于现有漫画源管理 API 下，没有新增顶级后台入口或改变页面布局：

- `GET /api/admin/venera-sources/export`：不包含私有状态的配置下载。
- 原 `POST /api/admin/venera-sources/import` 兼容 `{url}`，新增 `{file,mode:'merge'|'replace'}`。
- `PATCH /api/admin/venera-sources/:sourceId`：启停。
- `GET/PUT /api/admin/venera-sources/:sourceId/settings`：声明过的源设置。

接口接入现有管理 UI 尚待后台模块迁移一起验收，不能仅凭接口实现就声称按钮已全部接好。

## 剩余完成条件

### 写作台与路由样式隔离（已验收）

`/admin/write` 已迁入 Next 原生页面。站点页面与独立写作台分别置于 `(site)`、`(writer)` 路由组，URL 不变；写作台只加载原有 Tailwind 基础样式和原 scoped/global 样式，不继承站点全局布局。保留桌面/手机编辑、预览、文章侧栏、设置、字体与草稿流程。旧工具函数改为模块，事件、请求和计时器由页面生命周期回收；仍含命令式控制器，不声称已完成全量类型化。

补上刷新/离开页面时的本地草稿同步，避免原五秒自动保存尚未触发时丢失输入；不额外提交服务端文章。验收：生产构建和完整生产浏览器 82/82、前端单元 10/10、旧 UI 哈希核查通过。新增桌面/手机写作台对照、草稿请求与自动保存、输入后立即刷新恢复测试；路由组调整后的离线漫画启动回归也通过。其他站点页面、数据库迁移和源安全隔离仍未完成。

后续补验：手动保存和载入文章时取消待触发的自动保存，避免已提交后重新生成临时稿；本地草稿补存标签、系列、排序和配乐，并兼容不含这些字段的旧草稿。存储写入失败给出提示，不误报本地保存成功。此小批通过写作台定向浏览器 5/5 与类型检查；完整生产回归已包含这两项场景。

### 后台面板与漫画源文件（已验收）

`/admin` 已成为 Next 原生页面：原 36 个面板/弹窗拆为独立 React 视图，核心操作按媒体、音乐、文章、设置、评论、内容、备份和外壳分组。DOM 控制器保留原交互，但请求、事件、计时器、临时节点和跨模块通知归页面生命周期管理；取消旧请求不再误报或清掉新登录状态。现阶段控制器仍含命令式 DOM 与 JavaScript，不能称为全量类型化/纯 React 状态重写。

现有漫画源导入/导出按钮支持 Venera 配置和旧接口源；组合备份保持旧 bundle 格式并扩展 venera 字段，只包含漫画接口源、不导出其他业务默认源或 Venera 登录会话。导入 Venera 前确认同地址更新，保留其他仓库/书架；多文件导入中途失败明确报告此前已完成内容，不声称跨两类接口原子事务。直接访问 `#manga-sources` 也加载 Venera；没有新增独立顶级源管理入口。Venera 单源启停/设置的专门 UI 尚未接入，不能仅凭后端接口声称完成。

验收：生产构建、完整生产浏览器 77/77、前端单元 10/10、原 UI 哈希检查通过。新增后台登录的桌面/手机/双主题配对截图；19 个业务面板分别做桌面/手机对照；原按钮 Venera 导入/导出往返、登录/漫画设置保存/退出不重复提交。样例 API 不写真实数据，空数据界面对照不替代所有上传/删除/恢复业务的真实数据库回归。

本批验收时写作台尚未迁移，现已由上方写作台批次覆盖；其他站点页面、数据库目标迁移和源执行隔离仍继续推进，不把后台迁移视为全站完成。

### 本机 GitHub 网络恢复

2026-09-07 直连 GitHub 连续出现连接重置/超时；只读确认运行中的 Clash/Mihomo 监听 `127.0.0.1:7897` 后，以单次命令 `-c http.proxy=http://127.0.0.1:7897` 恢复推送。仍用 HTTPS + GCM 已保存令牌和 `credential.interactive=never`，没有修改系统/全局代理或保存凭据到仓库。该端口是本机当次发现值，其他机器不能盲目套用。

### 阅读中心与离线章节（已验收）

`/reading` 和 `/sw.js` 已迁入 Next；阅读中心继续使用原有私人设备、时间线、小说/漫画筛选、导航、页脚与音乐控件。原动态记录的实际样式保持不变，没有激活旧 Astro 从未生效的 scoped CSS。

离线按钮等待独立消息端口确认资源实际写入，失败不误报成功；下载 HTML、图片、样式和 React Flight 引用的启动脚本，最多 800 个资源、8 路并发。离线媒体仅读取用户明确保存的缓存，管理/私人 API 和 Flight 请求不进入通用缓存。保留已有阅读缓存和其他应用缓存。

验收：生产构建通过；完整生产浏览器 69/69（新增 4 组阅读中心界面对照、1 项真实断网重新打开漫画/显示图片/切换模式/翻页）；前端单元 8/8；UI 哈希无新增变化。断网用例是隔离本地漫画样例，不代表所有外部图片源或跨站 iframe 均可离线，也不代表整本漫画自动下载。

### 漫画阅读器与进度模块（已验收）

源章节阅读、本地漫画章节均已迁入 Next 原生路由，不再回退到 Astro 阅读器。保留原导航/音乐控件、连续/单页/双页、左右阅读方向、目录、背景/宽度/间距/页码设置、移动工具条和跨设备进度冲突提示。浏览器事件/计时器由组件生命周期清理；首次恢复不覆盖随后到达的翻页操作。原漫画阅读 URL 和图片上下文保持不变。

后端阅读进度抽离为独立 repository/API：双页模式可持久化，拒绝把其他漫画的章节写到当前条目，保存及版本校验在事务中完成。不删除旧数据，不改表结构。

验收：生产构建通过；完整生产浏览器 64/64，通过新旧阅读器桌面/手机/主题/三种本地模式截图及交互；前端单元 5/5；后端漫画格式导入回归通过；图片引擎 17/17、源持久化 4/4、进度模块 2/2；原 UI 哈希仍无新增变更。滚动截图先执行到底再回首的相同交互，等待页码和位置稳定，不遮蔽业务内容。

仍有限制：全站非漫画页面、后台迁移尚未完成；当前导航/音乐逻辑已提取生命周期控制器，但未表示所有旧全局业务脚本已重写。数据库仍为 SQLite，源执行隔离仍需后续实施；源登录仍是共享实例。

### 漫画详情、漫画架与收藏（已验收）

Next 原生路由新增源详情、本地/网络条目详情、漫画架、排行榜；保留原有 DOM/样式、设备存储键、收藏与书架操作、筛选/排序/列表记忆、卷章节目录及继续阅读。源详情动态路由显式处理编码标识；搜索表单支持 hydration 前原生提交，避免刚跳转后回车丢失查询。

验收：Next 生产构建通过；生产模式浏览器 52/52 通过（包含原有回归及新增详情/漫画架/排行榜截图、收藏刷新恢复、继续阅读）；前端单元测试 4/4；原 UI 哈希校验没有新增变更。截图仍使用原阈值，没有遮盖业务区域或放宽差异限制。旧阅读器仍由兼容路由提供，此批不把它计作迁移完成。全站迁移和数据库/源执行隔离仍未完成。

1. 所有漫画详情、阅读器、漫画架和收藏交互迁移，桌面/手机/主题/异常状态保持一致。
2. 全站其他页面及管理/写作界面迁移，不通过旧站代理充数。
3. 后端业务边界拆分；数据库目标实现及备份迁移演练；记录/关联/进度/文件核对。
4. 源执行真正的独立受限服务、网络出口和凭据隔离。
5. 全站功能与视觉回归、生产构建、本地可运行，旧兼容依赖退出。
6. GCM 令牌推送所有通过验证的修改；不得把未通过验证的结果报告为完成。
